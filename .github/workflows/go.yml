name: Go

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.20"

      - name: Build Linux
        env:
          GOOS: linux
          GOARCH: amd64
        run: go build -o dist/myapp-linux-amd64 ./...

      - name: Build Windows
        env:
          GOOS: windows
          GOARCH: amd64
        run: go build -o dist/myapp-windows-amd64.exe ./...

      - name: Build macOS
        env:
          GOOS: darwin
          GOARCH: amd64
        run: go build -o dist/myapp-darwin-amd64 ./...

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: go-builds
          path: dist/
